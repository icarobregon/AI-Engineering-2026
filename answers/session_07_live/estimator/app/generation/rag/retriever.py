"""Semantic retriever — RESERVED FOR SESSION 8.

Queries the vector store (``app.generation.rag.store``) by cosine similarity,
applies metadata/access-control filtering and returns ranked chunks with their
source citations to feed the estimation prompt. Empty until Session 8.
"""
